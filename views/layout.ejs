<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Quiz</title>
    
    <link rel='stylesheet' type="text/css"
      href='/stylesheets/style.css' />

    <link rel='stylesheet' type="text/css"
      media='only screen and (min-width: 530px) and (min-device-width: 481px)'
      href='/stylesheets/wide.css' />

    <link rel='stylesheet' type="text/css"
      media='only screen and (max-width: 480px)'
      href='/stylesheets/smartphone.css' />

    <link rel="icon" href="favicon.ico" type="image/x-icon" />
  </head>
  <body>

  <div id="page-wrap">
    <header class="main" id="h1">
      <% if (!session.user) {%>
           <form method="get" action="/login">
            <h2>Quiz<span>: El Juego de las Preguntas</span>    
              <button type="submit">
                Iniciar Sesión
              </button>
            </h2>  
          </form>
      <% } else { %>
           <form method="post" action="/logout?_method=delete">
            <h2>Quiz<span>: El Juego de las Preguntas</span>
              <button type="submit">
                Cerrar Sesión
              </button>              
            </h2>
            Usuario: <%= session.user.username %>
          </form>
      <% } %>

    </header>

    <nav class="main" id="n1" role="navigation">
      <span><a href="/">Inicio</a></span>
      <span><a href="/quizes">Preguntas</a></span>
      <span><a href="/author">Créditos</a></span>
    </nav>

    <section class="main" id="s1">
      <% if (errors.length) { %>
        <span id='ErrorMsgs'>Corregir errores:<br/>
          <%for (var i in errors) { %>
            <span> <%= errors[i].message %> </span><br/>
          <% } %>
        </span>
      <% } %>

      <div><%- body %></div>
    </section>

    <footer class="main" id="f1">
      <a href="http://es.wikipedia.org/wiki/Quiz">¿ Qué es un Quiz ?</a>
      <p>
      <a href="https://github.com/gsyjdcg/quiz">Proyecto GitHub</a>
    </footer>
  </div>
  </body>
</html>
